# Pi-Hub Features Tracker

## Feature Status

- ✅ Dynamic sidebar groups with drag-and-drop reordering
- ✅ Pi-Hub logger (`LoggerContext`)
- ✅ Service health panels (`HealthPanel`, `useHealthCheck`)
- ✅ Group management in Edit Layout, extra URLs, "Open in Browser"
- ✅ Dashboard modernization — compact cards, widget management (`WidgetPanel`, `WidgetEditor`, `WidgetPlaceholder`)
- ✅ Service-specific links with tabbed console view (`ServiceConsole`)
- ✅ Configuration retention fix (`App.jsx`, v1.5.6)
- ✅ Editor-specific files (`.vscode/`, `*.code-workspace`) excluded from version control
- ✅ Project slash-commands (`clean-project`, `commit-code`) versioned under `.claude/commands/`

## Next Planned Features (Phase 2+)

- No backlog items currently tracked — populate as new ideas surface in future sessions.
