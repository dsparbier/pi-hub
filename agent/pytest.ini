[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: touches a real Docker socket or the host — skipped in CI by default
filterwarnings =
    ignore::DeprecationWarning
